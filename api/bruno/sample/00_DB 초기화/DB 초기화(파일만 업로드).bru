meta {
  name: DB 초기화(파일만 업로드)
  type: http
  seq: 1
}

post {
  url: {{url}}/db/init
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  attach: @file()
  ~initData: @file(D:\likelion\febc-api\APIServer\api\dbinit\openmarket\data.json)
}

settings {
  encodeUrl: true
  timeout: 0
}
