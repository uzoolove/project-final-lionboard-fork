# 2장 프로젝트 시작
* 깃허브 레포지토리 생성부터 프로젝트 배포까지

# 1 깃허브 레포지토리 생성
## 1.1 깃허브 접속
- https://github.com 접속
  - 로그인

## 1.2 레포지토리 생성
- New(Repository)
  - Repository name: `project-final-lionboard`
  - Description: 멋쟁이 사자처럼 FrontEnd BootCamp 15기 파이널 프로젝트 샘플 라이언 보드
  - Choose visibility: Public 선택
  - Create repository

# 2 프로젝트 생성
## 2.1 프로젝트 폴더 생성
- 프로젝트를 저장할 폴더 생성 후 생성한 폴더로 이동
- 예) c:\febc15\project-final

## 2.2 Next.js 프로젝트 초기화
- project-final 폴더에서 실행
- 개인 실습일 경우 프로젝트명은 `lionboard`
- 파이널 팀 프로젝트일 경우 프로젝트명은 `final-팀번호-프로젝트명`
  + 예시: `flnal-00-lionboard`

  ```sh
  npx create-next-app@latest 프로젝트명

  ? Would you like to use the recommended Next.js defaults? » Yes, use recommended defaults 
  ...
  ```

## 2.3 생성한 프로젝트를 VSCode로 오픈
```sh
# 개인 프로젝트 실습일 경우
code lionboard
# 파이널 팀 프로젝트일 경우
code final-팀번호-프로젝트명
```

## 2.4 기본 파일 정리
- public 하위 파일 전체 삭제

- app 하위 파일 정리
  - global.css 파일 내용을 tailwindcss import 구문만 남기고 제거
  
    ```css
    @import "tailwindcss";
    ```

  - layout.tsx 수정

    ```tsx
    import "./globals.css";

    export default function RootLayout({
      children,
    }: Readonly<{
      children: React.ReactNode;
    }>) {
      return (
        <html lang="ko">
          <body>
            { children }
          </body>
        </html>
      );
    }
    ```

  - page.tsx 수정

    ```tsx
    export default function Home() {
      return (
        <h1>라이언 보드 v.01</h1>
      );
    }
    ```

## 2.5 개발 서버 실행
```sh
npm run dev
```

## 2.6 서버 접속 테스트
- 터미널의 안내 메세지에 따라 웹 브라우저로 접속
  - http://localhost:3000

# 3 깃허브 레포지토리에 push

## 3.1 Git 초기화
- 개발 서버 중지 후 실행

  ```sh
  git init
  ```

## 3.2 Git 원격 레포지토리 연결
```sh
git remote add origin https://github.com/[레포지토리 생성한 계정]/project-final-lionboard.git
```
  
## 3.3 commit & push
```sh
# 현재 폴더를 포함한 하위의 모든 변경사항을 스테이징 영역에 추가
git add .
# 스테이징 영역에 있는 변경사항을 커밋으로 기록
git commit -m "initial commit"
# 브랜치 이름을 강제로 변경(master일 경우 main으로)
git branch -M main
# 업스트림을 origin/main으로 지정한후 push
git push -u origin main
# 만약 원격 레포지토리에 .gitignore 같은 파일이 이미 있어서 충돌날 경우 강제로 push
git push -u origin main --force
```

# 4 프로젝트 배포

- 배포란?
  - 개발이 완료된 프로젝트(웹사이트나 애플리케이션)를 사용자들이 실제로 사용할 수 있도록 서버나 호스팅 플랫폼에 업로드하는 과정
  - 배포가 완료되면 사용자는 해당 애플리케이션에 접근하고 실행할 수 있음

## 4.1 빌드

### 4.1.1 배포 파일 생성
- 지정한 폴더에 프로덕션 배포용 파일 생성
- Next.js 프로젝트는 .next 폴더에 배포 파일이 생성됨

  ```sh
  npm run build
  ```
    
### 4.1.2 배포 파일 로컬 테스트
- 서버 구동

  ```sh
  npm start
  ```

- 터미널의 안내 메세지에 따라 웹 브라우저로 접속
  - http://localhost:3000

## 4.2 서비스 배포
- Next.js를 만든 Vercel에 배포

### 4.2.1 Vercel 접속
- https://vercel.com
  - GitHub 계정으로 로그인
  - GutHub 연동 과정에서 Import Git Repository가 나오면 project-final-lionboard 레포지토리 추가

### 4.2.2 Vercel에 프로젝트 생성
- Overview 화면에서 Add New... > Project
- Import Git Repository에서 project-final-lionboard 옆의 Import 선택
  - Vercel에 깃허브 로그인 과정에서 레포지토리를 연동하지 않았을 경우 Continue with GitHub 선택해서 GitHub 연동하고 project-final-lionboard 추가
- Vercel Team: 만들어둔 팀이 없을 경우 Create a Team 선택 후 Team Name 입력해서 생성
- ProjectName: lionboard
- Deploy
- 배포 완료 메세지가 보이면 Continue to Dashboard 선택

### 4.2.3 테스트
- 프로젝트 대시보드의 Domains에서 접속 주소 확인
- Visit 클릭해서 접속 테스트
- Vercel 프로젝트와 연결된 GitHub 레포지토리에 push 되면 자동으로 재배포 됨
